[project]
name = "trl-grpo-example"
version = "0.1.0"
description = "GRPO + TRL coding model training on Modal"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "modal>=0.73.0",
]
